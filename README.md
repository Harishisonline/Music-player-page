# Music Player Page

Static HTML/CSS music page inspired by a modern music player layout.

## Audio
The page expects an `espresso.mp3` file in the project root. Add the audio file you’re licensed to use, or update the `<audio>` tag in `index.html` to point to a hosted file URL.

## Run locally
Open `index.html` in your browser.
